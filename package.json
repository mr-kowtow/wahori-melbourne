{
  "name": "wahori-melbourne",
  "version": "1.0.0",
  "description": "Wahori Melbourne — bilingual housing landing page",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./style.css --minify",
    "dev": "tailwindcss -i ./input.css -o ./style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
